action (FindPodcast) {
  type (Search)
  description (Finds podcast audio item using a searchTerm)
  collect {
    input (searchTerm) {
      type (SearchTerm)
      min (Optional) max (One)
    }
  }
  output (PodcastAudio)
}
